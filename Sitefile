
SITE.user       = 'alexbcoles'
SITE.host       = 'nbgit.org'
SITE.remote_dir = '/users/home/alexbcoles/domains/nbgit.org/web/public'
#SITE.rsync_args = %w(-av --delete)

task :default => :build

desc 'deploy the site to the webserver'
task :deploy => [:build, 'deploy:rsync']


# EOF
